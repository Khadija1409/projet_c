#include <stdio.h>

int main(){
    intx ,n;
    do{
    printf("saisir n entiers:");
    scanf("%d",&n);
    }while(n<0);
    for(int i=0;i<n,i++){
        do{
            printf("veuillez saisir un entier negatif:");
        scanf("%d",x);
        }while(x>0);
        if(x%2==0){
            somme+=x;
            cpp++;
        }

    }
    moy=somme/cpt;
return 0;
}
